var newsListVM = {
    namespace: "newsListVM",
    init: function() {
    }
};

$(document).ready(function() {
    newsListVM.init();
});

// Thấy tinh nguyên, giữ mộc mạc, bớt suy nghĩ, giảm ham muốn - Lão tử
